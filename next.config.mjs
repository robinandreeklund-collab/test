/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Standalone output keeps the Render image small and boot fast.
  output: 'standalone',
};

export default nextConfig;
